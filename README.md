# MK Cli Tool

This is mk tool that combines `touch` and `mkdir` for smart project files creation.
To extend its use you can also quickly initialize project folder for GO with boilerplate structure.

In the future support for Python project initialization will be added.
